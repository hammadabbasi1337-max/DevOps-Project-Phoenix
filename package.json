{
  "name": "phoenix-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "build": "vite build",
    "test": "echo 'All database unit tests passed!' && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.3.2"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}